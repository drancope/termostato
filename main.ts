let temperatura = 0
basic.forever(function () {
    temperatura = input.temperature()
    if (true) {
        pins.digitalWritePin(DigitalPin.P0, 1)
    } else if (false) {
    	
    } else {
    	
    }
})
